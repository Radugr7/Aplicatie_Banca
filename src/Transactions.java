public class Transactions {

    /*public Transactions(User user, int dollars){
        if (user.isLogin() == true) {
            user.setBalance(user.getBalance() + dollars);
            System.out.println(user.getUserName() + " puts " + dollars + "$ in his Bank Account." +
                    "\nHis ballance is " + user.getBalance() + "$.");
        }
        else
            System.out.println("The transaction can't be made because the user is not logged in.");
    }*/
}
